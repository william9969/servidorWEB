import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'interfaz_practica_sistemas_distribuidos_esb_soa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
